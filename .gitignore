node_modules
dist
src/**/*.js

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Seeded design canvas: ~2.7 MB of generated editor payload, rebuilt from
# design/*.dc.html by design/build.mjs plus the canvas seeder.
design/trinette-restyle.html

# Iosevka release archive and the TTF extracted from it, both rebuildable by
# `make fonts`. The two built .woff2 under assets/fonts are checked in on
# purpose: a build should not need 84 MB of network and a python toolchain to
# produce two files that change only when IOSEVKA_VERSION does.
.fontbuild/
